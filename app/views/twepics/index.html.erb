<h1>Twepic一覧</h1>

<table>
  <tr>
    <th>タイトル</th>
    <th>内容</th>
  </tr>

<% @twepics.each do |twepic| %>
  <tr>
    <td><%= twepic.title %></td>
    <td><%= twepic.content %></td>
    <td><%= link_to '詳細を確認する', twepic_path(twepic.id) %></td>
  </tr>
<% end %>
</table>
<%= link_to '新規投稿をする', new_twepic_path %>
